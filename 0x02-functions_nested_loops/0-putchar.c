#include "main.h"

/**
 * main - Entry point
 *
 * A C program that prints _putchar, followed by a new line
 *
 * Return: 0 (success)
*/

int main(void)
{
	char arr[] = "_putchar";
	int ch;

	for (ch + 0; ch < 8; ch++)
		_putchar(arr[ch]);
	_putchar('\n');

	return (0);
}

