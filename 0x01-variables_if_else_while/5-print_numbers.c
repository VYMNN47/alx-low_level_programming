#include <stdio.h>
/**
 * main - Entry point
 *
 * A C program that prints all single digit numbers of base 10
 *
 * Return: 0 (Success)
*/

int main(void)
{
	int digit = 0;

		while (digit <= 0)
		{
			printf("%d", digit);
			digit++;
		}
		printf("\n");

		return (0);
}

